const root=document.documentElement,button=document.getElementById('theme'),icon=document.getElementById('themeIcon');const saved=localStorage.getItem('neo-theme');const dark=window.matchMedia('(prefers-color-scheme: dark)').matches;setTheme(saved||(dark?'dark':'light'));button.addEventListener('click',()=>setTheme(root.dataset.theme==='dark'?'light':'dark'));function setTheme(t){root.dataset.theme=t;localStorage.setItem('neo-theme',t);icon.textContent=t==='dark'?'light_mode':'dark_mode'}document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(!t)return;e.preventDefault();t.scrollIntoView({behavior:'smooth'})}));
